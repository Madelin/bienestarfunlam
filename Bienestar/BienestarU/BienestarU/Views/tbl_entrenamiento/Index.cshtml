@model IEnumerable<BienestarU.Models.tbl_entrenamiento>

@{
    ViewBag.Title = "Index";
}

<h2>ENTRENAMIENTOS</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.horario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.lugar)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbl_deporte.nombre_deporte)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.horario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.lugar)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_deporte.nombre_deporte)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_entrenamiento }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_entrenamiento }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_entrenamiento })
        </td>
    </tr>
}

</table>
